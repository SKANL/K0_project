{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main window only. No filesystem, shell, HTTP, or browser plugin capability is granted. Product IPC exposes only named contract commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
