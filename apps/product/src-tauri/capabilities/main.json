{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Main window only; no filesystem capability is granted. The sole IPC command returns the fixed $APPDATA/K0 boundary.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
